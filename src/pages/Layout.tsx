import { Outlet } from "react-router-dom";
import Header from "../components/Header";
import { Outlet } from "react-router-dom"

const Layout = () => {
  return (
    <div>
      <Header/>
        <Outlet />
    </div>
  );
};

export default Layout;

        Layout
        <Outlet/>
    </div>
  )
}

export default Layout
